<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/numara_detail"
    tools:context=".view.NumberDetailPage">

    <ProgressBar
        android:id="@+id/numara_bar"
        android:layout_marginTop="100dp"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:indeterminateOnly="false"
        tools:progress="50"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:progressDrawable="@drawable/circle"
        android:progress="75">
    </ProgressBar>

    <TextView
        android:id="@+id/tv_yuzdelik"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="%100"
        android:textSize="60sp"
        app:layout_constraintTop_toTopOf="@+id/numara_bar"
        app:layout_constraintBottom_toBottomOf="@+id/numara_bar"
        app:layout_constraintStart_toStartOf="@+id/numara_bar"
        app:layout_constraintEnd_toEndOf="@+id/numara_bar"
        />
</androidx.constraintlayout.widget.ConstraintLayout>